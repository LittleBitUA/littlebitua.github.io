# План імплементації: 4 нові функції для сайту локалізації

## 📋 Загальний огляд

Реалізуємо 4 основні функції:
1. **Детальні сторінки проєктів** - окрема сторінка для кожної гри з повною інформацією
2. **SEO оптимізація** - structured data, динамічні OG теги, покращені мета-теги
3. **Інтерактивний прогрес-трекер** - графіки, timeline, статистика
4. **Пошук + розширені фільтри** - live search, сортування, додаткові фільтри

---

## 🗂️ Структура даних: розширення games.ts

### Поточний стан
```typescript
export interface Game {
  id: string;
  title: string;
  series?: string;
  description: string;
  cover: string;
  progress: number;
  status: GameStatus;
  // ... існуючі поля
}
```

### Додати нові поля

```typescript
export interface Game {
  // ... існуючі поля ...

  // Для детальних сторінок
  screenshots?: string[];           // Масив шляхів до скріншотів перекладу
  installInstructions?: string;     // Markdown з інструкціями встановлення
  team?: TeamMember[];              // Команда перекладачів
  detailedDescription?: string;     // Розширений опис для detail page
  releaseDate?: string;             // Дата релізу локалізації (якщо done)
  gameYear?: number;                // Рік випуску оригінальної гри

  // Для прогрес-трекера
  progressHistory?: ProgressEntry[]; // Історія змін прогресу
  stats?: {
    totalLines?: number;             // Кількість рядків тексту
    translatedLines?: number;
    totalWords?: number;
    totalCharacters?: number;
  };

  // Для пошуку/фільтрації
  genre?: string[];                  // Жанри гри
  platform?: string[];               // Платформи (PC, PlayStation, Xbox)
  translationSize?: 'small' | 'medium' | 'large';  // Розмір перекладу
}

// Нові типи
export interface TeamMember {
  name: string;
  role: string;  // "Перекладач", "Редактор", "Тестувальник"
  link?: string; // Посилання на профіль (Telegram, GitHub)
}

export interface ProgressEntry {
  date: string;      // ISO date string
  progress: number;  // 0-100
  milestone?: string; // Опис мілстоуна (якщо є)
}
```

---

## 📁 Файли для створення

### 1. Детальні сторінки проєктів

#### `src/pages/projects/[slug].astro`
**Призначення**: Динамічний маршрут для кожної гри
**Функції**:
- Використовує `getStaticPaths()` для генерації всіх сторінок
- Відображає детальну інформацію про проєкт
- Галерея скріншотів
- Інструкції встановлення
- Команда перекладачів
- Історія прогресу (міні-графік)

**Структура сторінки**:
```
- Hero з обкладинкою гри
- Прогрес-бар і статистика
- Детальний опис
- Галерея скріншотів (до/після)
- Інструкції встановлення
- Команда проєкту
- Посилання на завантаження
- Навігація до наступного/попереднього проєкту
```

#### `src/components/ProjectDetailHero.astro`
Верхня секція detail page з великою обкладинкою

#### `src/components/ScreenshotGallery.astro`
Галерея з lightbox для скріншотів

#### `src/components/InstallInstructions.astro`
Компонент з інструкціями (підтримує Markdown)

#### `src/components/TeamList.astro`
Список членів команди з аватарками/посиланнями

---

### 2. Пошук та розширені фільтри

#### `src/components/SearchBar.astro`
**Призначення**: Live search по назві гри
**Функції**:
- Інпут з іконкою лупи
- Real-time пошук (debounced)
- Показує кількість знайдених результатів
- Очистка пошуку

#### `src/components/AdvancedFilters.astro`
**Призначення**: Розширена панель фільтрації
**Функції**:
- Фільтр по року випуску (slider або select)
- Фільтр по жанру (multi-select)
- Фільтр по розміру перекладу (small/medium/large)
- Фільтр по платформі
- Сортування:
  - По прогресу (зростання/спадання)
  - По даті оновлення
  - По алфавіту
  - По року випуску

#### Модифікація `src/pages/index.astro`
- Замінити існуючий FilterBar на SearchBar + AdvancedFilters
- Додати JavaScript для координації пошуку і фільтрів

#### Модифікація `src/components/FilterBar.astro`
**Варіант 1**: Розширити існуючий FilterBar новими можливостями
**Варіант 2**: Створити новий AdvancedFilters і замінити FilterBar

**Рекомендація**: Варіант 1 - розширити існуючий компонент

---

### 3. Прогрес-трекер з історією

#### `src/components/ProgressTracker.astro`
**Призначення**: Інтерактивні графіки прогресу
**Функції**:
- Графік загального прогресу в часі (line chart)
- Графік прогресу окремих проєктів
- Timeline релізів (вертикальна лінія з датами)
- Статистика (загалом перекладено рядків/слів)

**Бібліотека**: Chart.js (легка, підтримує SSR)
```bash
npm install chart.js
```

#### `src/components/Timeline.astro`
**Призначення**: Часова шкала релізів і мілстоунів
**Функції**:
- Вертикальна/горизонтальна лінія
- Позначки з датами релізів
- Описи мілстоунів

#### `src/components/StatsCards.astro`
**Призначення**: Картки зі статистикою
**Функції**:
- Загальна кількість рядків перекладено
- Кількість завершених проєктів
- Активність по місяцях (heatmap як на GitHub)

#### Модифікація `src/pages/index.astro`
Додати нову секцію "Прогрес команди" після секції проєктів

---

### 4. SEO оптимізація

#### Модифікація `src/layouts/Layout.astro`
**Поточний стан**: Добра база (OG теги, Twitter Card, основні мета-теги)
**Що додати**:
1. **Динамічні OG теги**:
   - Приймати `ogImage`, `canonicalUrl` як props
   - Генерувати `og:url` динамічно
   - Додати `og:type` (website/article)

2. **Structured Data (JSON-LD)**:
   - Розширити існуючу Organization schema
   - Додати `Product` schema для кожної гри на detail page
   - Додати `BreadcrumbList` для навігації
   - Додати `WebPage` schema з основними даними

#### `src/utils/seo.ts`
**Призначення**: Утиліти для генерації SEO даних
**Функції**:
```typescript
// Генерація structured data для гри
export function generateGameStructuredData(game: Game): string;

// Генерація OG опису з обмеженням символів
export function generateOGDescription(text: string, maxLength: number): string;

// Генерація canonical URL
export function getCanonicalUrl(path: string): string;

// Генерація breadcrumbs schema
export function generateBreadcrumbs(items: BreadcrumbItem[]): string;
```

#### `public/sitemap.xml` (автоматична генерація)
**Поточний стан**: Автоматично генерується @astrojs/sitemap
**Після додавання detail pages**: Автоматично включить `/projects/*` маршрути
**Дія**: Немає - працює "з коробки"

#### `public/robots.txt`
**Поточний стан**: Вже існує і правильно налаштований ✓
**Дія**: Немає

---

## 🔧 Технічні рішення

### 1. Бібліотеки для додавання

```json
{
  "dependencies": {
    "chart.js": "^4.4.0",           // Графіки прогресу
    "fuse.js": "^7.0.0"              // Fuzzy search для пошуку
  }
}
```

### 2. Lightbox для галереї
**Варіанти**:
- **PhotoSwipe** (рекомендація) - легкий, responsive, touch-friendly
- **GLightbox** - менший розмір
- **Vanilla JS** - власна реалізація (найлегша)

**Рекомендація**: Власна реалізація з `<dialog>` HTML5 (нативний, без залежностей)

### 3. Markdown для інструкцій
Astro підтримує markdown "з коробки" через:
- `@astrojs/markdown-remark` (вже встановлено)
- Можна використовувати `.md` файли або рендерити markdown в компонентах

---

## 📝 План імплементації (покроково)

### ЕТАП 1: Підготовка даних (Foundation) 🏗️

**Мета**: Розширити структуру даних без порушення існуючого коду

**Кроки**:
1. ✅ Додати нові поля до `Game` interface в `src/data/games.ts`
2. ✅ Додати приклад даних для 2-3 ігор:
   - `progressHistory` з фейковими даними для демонстрації
   - `screenshots` (можна використовувати існуючі зображення спочатку)
   - `installInstructions` з markdown
   - `genre`, `gameYear`, `platform`, `translationSize`
3. ✅ Переконатись що існуючий код не ламається (всі нові поля опціональні)

**Файли**:
- `src/data/games.ts` (модифікація)

**Тестування**: `npm run build` повинно пройти успішно

---

### ЕТАП 2: SEO оптимізація 🔍

**Мета**: Покращити SEO перед додаванням нових сторінок

**Кроки**:
1. ✅ Створити `src/utils/seo.ts` з утилітами
2. ✅ Модифікувати `src/layouts/Layout.astro`:
   - Додати props: `ogImage?`, `canonicalUrl?`, `structuredData?`
   - Зробити canonical URL динамічним
   - Додати conditional rendering для різних типів structured data
3. ✅ Додати `BreadcrumbList` schema для майбутніх detail pages
4. ✅ Тестувати OG теги з https://www.opengraph.xyz/

**Файли**:
- `src/utils/seo.ts` (новий)
- `src/layouts/Layout.astro` (модифікація)

**Тестування**:
- Build успішний
- OG preview коректний
- Schema validator (schema.org) валідує JSON-LD

---

### ЕТАП 3: Пошук та фільтри 🔎

**Мета**: Покращити UX на головній сторінці

**Кроки**:
1. ✅ Встановити `fuse.js`: `npm install fuse.js`
2. ✅ Модифікувати `src/components/FilterBar.astro`:
   - Додати search input зверху
   - Додати нові фільтри (рік, жанр, платформа, розмір)
   - Додати сортування (dropdown)
3. ✅ Оновити JavaScript логіку:
   - Інтегрувати Fuse.js для fuzzy search
   - Координувати пошук + фільтри + сортування
   - Зберігати стан в URL params (опціонально, для bookmarking)
4. ✅ Додати індикатор "Знайдено X проєктів"
5. ✅ Додати кнопку "Скинути всі фільтри"

**Файли**:
- `src/components/FilterBar.astro` (модифікація)
- Можливо створити окремі: `SearchBar.astro`, `SortControls.astro`

**Тестування**:
- Пошук працює real-time
- Фільтри працюють в комбінації
- Сортування коректне
- Performance (debounce search input)

---

### ЕТАП 4: Детальні сторінки проєктів 📄

**Мета**: Створити detail pages для кожної гри

**Кроки**:
1. ✅ Створити `src/pages/projects/[slug].astro`
   - Імплементувати `getStaticPaths()`
   - Layout з hero секцією
   - Breadcrumbs навігація
2. ✅ Створити компоненти:
   - `ProjectDetailHero.astro` - верхня частина
   - `ScreenshotGallery.astro` - галерея з lightbox
   - `InstallInstructions.astro` - інструкції
   - `TeamList.astro` - команда
   - `ProjectNavigation.astro` - попередній/наступний проєкт
3. ✅ Додати структуровані дані для кожної гри (Product schema)
4. ✅ Додати посилання з GameCard на detail page
5. ✅ Створити lightbox для галереї (нативний `<dialog>`)

**Файли**:
- `src/pages/projects/[slug].astro` (новий)
- `src/components/ProjectDetailHero.astro` (новий)
- `src/components/ScreenshotGallery.astro` (новий)
- `src/components/InstallInstructions.astro` (новий)
- `src/components/TeamList.astro` (новий)
- `src/components/ProjectNavigation.astro` (новий)
- `src/components/GameCard.astro` (модифікація - додати посилання)

**Тестування**:
- Всі detail pages генеруються при build
- Навігація між проєктами працює
- Lightbox відкривається/закривається
- Responsive на мобільних
- SEO: canonical URLs, OG теги коректні

---

### ЕТАП 5: Прогрес-трекер 📊

**Мета**: Додати інтерактивні графіки та статистику

**Кроки**:
1. ✅ Встановити Chart.js: `npm install chart.js`
2. ✅ Створити `src/components/ProgressTracker.astro`:
   - Line chart загального прогресу
   - Multiple lines для топ 5 активних проєктів
   - Легенда з назвами ігор
3. ✅ Створити `src/components/Timeline.astro`:
   - Вертикальна timeline релізів
   - Позначки з датами та назвами ігор
4. ✅ Створити `src/components/StatsCards.astro`:
   - Картки з цифрами (total lines, words, characters)
   - Heatmap активності (опціонально)
5. ✅ Додати нову секцію на головну сторінку:
   ```astro
   <!-- Progress Analytics Section -->
   <section class="py-20">
     <ProgressTracker />
     <Timeline />
     <StatsCards />
   </section>
   ```

**Файли**:
- `src/components/ProgressTracker.astro` (новий)
- `src/components/Timeline.astro` (новий)
- `src/components/StatsCards.astro` (новий)
- `src/pages/index.astro` (модифікація - додати секцію)
- `src/utils/chartConfig.ts` (новий - конфігурація Chart.js)

**Тестування**:
- Графіки рендеряться коректно
- Responsive на мобільних
- Дані з `progressHistory` відображаються правильно
- Chart.js працює в Astro SSR (може потрібен `client:load`)

---

## 🎨 Дизайн-система (щоб все виглядало єдино)

### Кольори (з tailwind.config.js)
- **Accent**: `#6d5dfc` (фіолетовий)
- **Steam Blue**: `#1b9aaa` (бірюзовий)
- **Steam Green**: `#5cb85c` (зелений для done)
- **Surface**: темні відтінки сірого

### Компоненти
- **Картки**: `bg-white/[0.03]` з `border border-white/[0.06]`
- **Кнопки**: Gradient або solid з hover effects
- **Glassmorphism**: `backdrop-blur-md` для popup/modal
- **Анімації**: `transition-all duration-300` стандарт

### Типографія
- **Шрифт**: Inter (вже використовується)
- **Заголовки**: `font-black` або `font-extrabold`
- **Body**: `text-surface-200` або `text-surface-300`

---

## ⚠️ Потенційні ризики та рішення

### 1. Performance: Chart.js на SSR
**Проблема**: Chart.js потребує browser API (canvas)
**Рішення**: Використати `client:load` директиву в Astro
```astro
<ProgressTracker client:load />
```

### 2. Image optimization
**Проблема**: Великі зображення (1MB+) сповільнюють сайт
**Рішення**:
- Додати `@astrojs/image` пакет
- Або використати `sharp` для оптимізації при build
- Створити WebP версії

### 3. Progressистory дані
**Проблема**: Поки немає реальних історичних даних
**Рішення**:
- Створити фейкові дані для 2-3 ігор як приклад
- Додати коментар в games.ts як заповнювати progressHistory

### 4. Search performance
**Проблема**: Fuse.js може бути повільним на великих датасетах
**Рішення**:
- Debounce input (300ms)
- Ліміт результатів (20 ігор макс)
- Кешувати Fuse instance

### 5. Sitemap regeneration
**Проблема**: Потрібно перебілдити для оновлення sitemap
**Рішення**: Автоматично працює при `npm run build` - не проблема

---

## 📦 Залежності для встановлення

```bash
npm install chart.js fuse.js
```

**Опціонально** (якщо користувач захоче):
```bash
npm install @astrojs/image sharp  # Для оптимізації зображень
```

---

## ✅ Чеклист готовності

### До початку розробки
- [x] План створено
- [ ] Користувач підтвердив підхід
- [ ] Вирішено: використовувати Chart.js чи альтернативу?
- [ ] Вирішено: lightbox бібліотека чи нативний?

### Після імплементації
- [ ] Всі 4 функції реалізовано
- [ ] Build проходить без помилок
- [ ] Sitemap включає нові сторінки
- [ ] SEO validator показує коректні дані
- [ ] Mobile responsive працює
- [ ] Performance задовільний (Lighthouse score)

---

## 🚀 Очікуваний результат

**Після імплементації користувач отримає**:

1. **Detail Pages**: `/projects/yakuza-kiwami2/`, `/projects/judgment/` тощо
   - Повна інформація про кожну гру
   - Галерея скріншотів
   - Інструкції встановлення
   - Команда перекладачів

2. **Покращений SEO**:
   - Structured data для кожної гри
   - Динамічні OG теги
   - Краща видимість в Google

3. **Графіки прогресу**:
   - Інтерактивні charts
   - Timeline релізів
   - Статистика перекладів

4. **Потужний пошук**:
   - Live search по назві
   - Фільтри: жанр, рік, платформа, розмір
   - Сортування різними способами

**Час розробки**: ~4-6 годин для всіх 4 функцій (залежно від складності деталей)

---

## 📞 Питання до користувача перед стартом

1. **Graphіки**: Chart.js підходить? Чи є альтернативні побажання?
2. **Lightbox**: Нативний `<dialog>` чи конкретна бібліотека?
3. **Дані**: Чи є реальні дані progressHistory для якихось ігор, чи створити фейкові для демонстрації?
4. **Скріншоти**: Чи є скріншоти перекладів для додавання в галерею? Якщо ні - можна використовувати placeholder.
5. **Пріоритет**: Чи є якась функція, яку треба зробити в першу чергу?

---

**Готовий до старту! 🎯**
